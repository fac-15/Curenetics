import React from "react";
import "./button.css";

const Button = props => {
  //   console.log(props);
  // colour, button text and arrow
  const btnClass = `button`;

  return <button className={btnClass}>button text</button>;
};

export default Button;
